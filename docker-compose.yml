services:
  subtracker:
    build: .
    container_name: subtracker
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - subtracker-data:/app/prisma/data
    environment:
      - DATABASE_URL=file:/app/prisma/data/subtracker.db
      - AUTH_USERNAME=admin
      - AUTH_PASSWORD=admin123
      - JWT_SECRET=change-me-in-production
      - NEXT_PUBLIC_DEFAULT_CURRENCY=CNY
      - NEXT_PUBLIC_DEFAULT_LANGUAGE=zh
      - TZ=Asia/Shanghai

volumes:
  subtracker-data:
